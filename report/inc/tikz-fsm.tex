\usepackage{tikz}
\usetikzlibrary{automata,positioning,arrows,decorations.markings}
\tikzset{
% ->, % makes the edges directed
>=stealth, % makes the arrow heads bold
node distance=3cm, % specifies the minimum distance between two nodes. Change if necessary.
every state/.style={thick, fill=gray!10}, % sets the properties for each ’state’ node
initial text=复位, % sets the text that appears on the start arrow
myptr/.style={decoration={markings,mark=at position 1 with %
    {\arrow[scale=2,>=stealth]{>}}},postaction={decorate}}
}

\iffalse
    \begin{center}
    \begin{tikzpicture}
    \node[state, initial, accepting]              (1) {1.RESET};
    \node[state, below left of=1, below left=1]   (2) {2.PAUSE};
    \node[state, below right of=1, below right=1] (3) {3.PLAY};
    \node[state, below of=3]                      (4) {4.NEXT};
    \draw (1) edge[myptr, above, above left=3] node{初始化设置} (2)
          (2) edge[myptr, bend left, above] node{play\_pause} (3)
          (3) edge[myptr, bend left, below] node{play\_pause} (2)
          (2) edge[myptr, bend right, below left=1] node{next\_button} (4)
          (3) edge[myptr, bend left, right=1] node{next\_button} (4)
          (4) edge[myptr, bend left] node{} (3)
          (3) edge[myptr, bend right, right=3] node{song\_done} (1);
    \end{tikzpicture}
    \captionof{figure}{MCU状态转换示意图}
    \end{center}
\fi